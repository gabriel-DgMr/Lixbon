name: Compile & Release Tauri App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './App Folax/package-lock.json'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./App Folax/src-tauri -> target"

      - name: Install dependencies
        run: |
          cd "App Folax"
          npm install

      - name: Build Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: "***REMOVED-OLD-TAURI-KEY***"
        with:
          tagName: app-folax-v__VERSION__
          releaseName: 'App Folax v__VERSION__'
          releaseBody: 'Autogenerated desktop installers for Windows.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
          projectPath: './App Folax'

      - name: Upload compiled MSI to server
        shell: pwsh
        run: |
          $MSI_PATH = Get-ChildItem -Path "App Folax/src-tauri/target/release/bundle/msi/*.msi" | Select-Object -First 1
          if ($MSI_PATH) {
            Write-Host "Uploading $($MSI_PATH.Name) to remote server..."
            # Determinar la versión dinámicamente desde el package.json
            $PackageJson = Get-Content -Raw -Path "App Folax/package.json" | ConvertFrom-Json
            $Version = $PackageJson.version
            
            # Obtener los últimos 3 mensajes de commits como changelog real
            $GitCommits = git log -n 3 --pretty=format:"%s"
            if ($GitCommits) {
              $ChangelogArray = $GitCommits -split "`n"
              $ChangelogJson = ConvertTo-Json $ChangelogArray -Compress
            } else {
              $ChangelogJson = "[`"Compilación automática desde GitHub Actions`"]"
            }
            
            # Petición multipart form para subir el instalador a la API del servidor
            curl.exe -X POST `
              -F "version=$Version" `
              -F "channel=stable" `
              -F "title=App Folax v$Version" `
              -F "changelog=$ChangelogJson" `
              -F "file=@$($MSI_PATH.FullName)" `
              https://remote.datacentgbx.online/api/versions/upload
          } else {
            Write-Warning "No MSI installer found to upload."
          }
