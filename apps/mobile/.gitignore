# Expo/React Native: solo se versiona el JS + assets + configuración; la
# carpeta android/ la genera el CI con `npx expo prebuild --platform android`.
node_modules/
.expo/
android/
ios/
build/
dist/
*.log
